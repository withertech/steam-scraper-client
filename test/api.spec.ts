import { ScrapeApi } from '../src/api'
describe('api', () => {
	describe('myPackage', () => {
		// it('should return a string containing the message', () => {
		// 	const api = new ScrapeApi("http://localhost:8080");
		// 	api.scrape("", "").then((result) => {
		// 		// expect(result).toMatch(message);
		// 		console.log(result);
		// 	})
		// });
	});
});
